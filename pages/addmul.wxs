var filters = {
  toFix: function (value) {
    return value.toFixed(3)//此处2为保留两位小数
  },
  substr:function(v){
   return  v.substring(0, 7)+'...';
  }
}
module.exports = {
  toFix: filters.toFix,
  substr:filters.substr,
}